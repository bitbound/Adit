<Window x:Class="Adit.Windows.SSL"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Adit.Windows"
        mc:Ignorable="d"
        Title="Configure SSL" Height="300" Width="400" WindowStartupLocation="CenterOwner" Closing="Window_Closing">
    <StackPanel Margin="10">
        <TextBlock FontSize="16" FontWeight="Bold" Margin="0,0,0,10">SSL Setup</TextBlock>
        <TextBlock TextWrapping="Wrap">
            SSL encryption is important for securing data being sent outside your local network.  You should always use an encrypted connection when accessing computers outside your local network.
            <LineBreak></LineBreak>
            <LineBreak></LineBreak>
            You can create your own self-signed using <Hyperlink Click="Hyperlink_Click">PowerShell's New-SelfSignedCertificate cmdlet</Hyperlink>.
            <LineBreak></LineBreak>
            <LineBreak></LineBreak>
            After creating the certificate on the server machine, click the below button and select your certificate from the list.
        </TextBlock>
        <Button Margin="0,20,0,0" Height="30" Width="100" Click="ImportCert_Click" HorizontalAlignment="Center">Import Certificate</Button>
    </StackPanel>
</Window>
