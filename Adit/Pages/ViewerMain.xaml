<Page x:Class="Adit.Pages.ViewerMain"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Adit.Pages"
    mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="Adit Viewer" Background="White" Loaded="Page_Loaded">

    <Grid x:Name="mainGrid">
        <Grid x:Name="viewerFrame">
            
        </Grid>
        <Grid x:Name="controlsFrame">
            <StackPanel  HorizontalAlignment="Center" VerticalAlignment="Center" Width="150">
                <TextBlock Margin="0,0,0,5">Session ID:</TextBlock>
                <TextBox  Margin="0,0,0,15"></TextBox>
                <Button Style="{DynamicResource PrimaryButton}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Padding="5">Connect</Button>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
